# ict650-gpproject

The "signindemo" file is the database file.
The "signindemo 2" file is the main source code of Android studio file.
This project will link to a database when the user sign in into the apps by using Gmail.
All the user credentials, sign-in date, user agent (sign-in device) and lastly is their coordinates (location) will be stored into the database.
